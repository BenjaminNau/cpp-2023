/*
Write a program that prompts the user to input five decimal numbers.

The program should then add the five decimal numbers, convert the sum to the nearest integer, and print the result.
*/

#include <iostream>

using namespace std;

int main() {
    double num1, num2, num3, num4, num5; // initialize double values
    
    cout << "input five decimal numbers" << endl; // prompt user
    cin >> num1 >> num2 >> num3 >> num4 >> num5; 
    
    int roundedVal = static_cast<double>(num1 + num2 + num3 + num4 + num5 +.5); // function for rounding, utilizing truncation
    
    cout << roundedVal; // output
    return 0;
}
